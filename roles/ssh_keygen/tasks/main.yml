---
# tasks file for ssh_keygen
- name: setup ssh-keygen
  shell: echo n|ssh-keygen -q -t rsa -N $(echo {{ ssh_passphrase }} | openssl enc -aes-128-cbc -a -d -salt -pass pass:wtf) -f {{ ansible_env.HOME }}/.ssh/id_rsa
  ignore_errors: True
